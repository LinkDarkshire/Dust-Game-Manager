# Python Requirements for Dust Game Manager with VPN Support
# Add these dependencies to your existing requirements.txt

# Existing requirements (keeping these)
flask>=2.3.0
flask-cors>=4.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0
aiohttp>=3.8.0
aiofiles>=23.0.0
python-dotenv>=1.0.0

# New VPN-related requirements
psutil>=5.9.0          # For process monitoring and management
python-openvpn>=0.1.0  # OpenVPN Python library (if available)
asyncio-subprocess>=0.1.0  # Enhanced subprocess support for async operations

# VPN management libraries (choose one that works best)
openvpn-api>=0.3.0   # OpenVPN management library

# Enhanced logging and monitoring
watchdog>=3.0.0        # File system monitoring for config changes
schedule>=1.2.0        # Task scheduling for periodic status checks

# Network utilities
netifaces>=0.11.0      # Network interface information
ipaddress>=1.0.23      # IP address manipulation (usually built-in)

# Configuration and settings management
configparser>=5.3.0    # Configuration file parsing (usually built-in)
json5>=0.9.0          # Enhanced JSON parsing

# Error handling and retry logic
tenacity>=8.2.0        # Retry decorator with exponential backoff
backoff>=2.2.0         # Alternative retry library

# Optional: GUI components for settings (if needed)
# tkinter                # Usually comes with Python
# PyQt5>=5.15.0         # Alternative GUI framework
# wxPython>=4.2.0       # Another GUI option

# Development and testing
pytest>=7.4.0          # Testing framework
pytest-asyncio>=0.21.0 # Async testing support
mock>=5.1.0            # Mocking for tests

# Documentation
sphinx>=7.1.0          # Documentation generation
sphinx-rtd-theme>=1.3.0 # ReadTheDocs theme